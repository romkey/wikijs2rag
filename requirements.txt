# Wiki.js API + HTML scraping fallback
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Vector store
qdrant-client>=1.9.0

# Local embeddings
# fastembed uses ONNX Runtime instead of PyTorch — ~150 MB vs ~800 MB,
# supports the same model names as sentence-transformers.
fastembed>=0.4.0

# Optional: OpenAI embeddings backend
# openai>=1.30.0
